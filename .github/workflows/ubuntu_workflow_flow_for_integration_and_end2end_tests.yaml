# Operating System: Ubuntu
# Java Versions: Java 8 
# Tests: All tests (Integration, End-to-End)
# Reports: FailSafeReport
# DISABLED: Use main.yml consolidated workflow instead
name: JAVA IT, E2E tests with Maven in Linux

on:
  workflow_dispatch:  # Only run manually if needed
  # push:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - java: 8
            additional-maven-args: "-Pintegration-test-profile"
    name: Build with Java ${{ matrix.java }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
      - name: Cache Maven Packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-jdk-${{ matrix.java }}-${{ hashFiles('**/pom.xml','**/*.yml') }}
          restore-keys: ${{ runner.os }}-m2-
      - name: Build with Maven
        run: xvfb-run mvn verify ${{ matrix.additional-maven-args }}
        env:
          DOCKER_HOST: unix:///var/run/docker.sock
      - name: ArchiveFailSafeReport
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: failsafe-report-jdk-${{ matrix.java }}
          path: '**/target/failsafe-reports'

